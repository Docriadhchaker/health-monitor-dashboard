/**
 * Map explorer: world map with clustering and event markers.
 * Placeholder for Phase 1 — MapLibre + clustering in later phase.
 */
export function MapExplorerPage() {
  return (
    <div className="h-full flex items-center justify-center bg-[var(--color-bg-muted)]">
      <div className="text-center text-[var(--color-text-muted)]">
        <p className="text-sm">Map viewport (MapLibre + clustering)</p>
        <p className="text-xs mt-1">Placeholder — Phase 1</p>
      </div>
    </div>
  )
}
