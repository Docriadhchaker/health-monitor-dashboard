# Archives

This folder contains archived document copies that were previously at the repository root (e.g. `healthmonitor_*.md`). They are kept for reference; the canonical docs live in `docs/product/`, `docs/architecture/`, etc.
